[project]
name = "pytest-tut"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cards",
    "pytest>=9.0.3",
]

[tool.uv.workspace]
members = [
    "code/cards_proj",
]

[tool.uv.sources]
cards = { workspace = true }
